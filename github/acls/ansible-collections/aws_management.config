---
# NOTE(pabelanger): See https://developer.github.com/v3/repos/branches/#parameters-1
# for fields
- branch:
  name: main 
  enforce_admins: true
  name: main
  required_status_checks:
    contexts:
      - ansible/check
      - ansible/gate
  restrictions:
    apps:
      - ansible-zuul
